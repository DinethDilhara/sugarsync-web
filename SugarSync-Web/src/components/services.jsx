import React from "react";

export const Services = (props) => {
  return (
    <div id="services" className="text-center">
      <div className="container">
        <div className="section-title">
          <h2>Main Featuers</h2>
          <p>
          The system offers personalized meal and exercise recommendations, a meal nutrition calculator, automated daily reminders for medication, meals, and exercises, visual tracking tools, blood sugar level monitoring, and secure sharing of health data with doctors and caregivers for comprehensive health management.
          </p>
        </div>
        <div className="row">
          {props.data
            ? props.data.map((d, i) => (
                <div key={`${d.name}-${i}`} className="col-md-4">
                  {" "}
                  <i className={d.icon}></i>
                  <div className="service-desc">
                    <h3>{d.name}</h3>
                    <p>{d.text}</p>
                  </div>
                </div>
              ))
            : "loading"}
        </div>
      </div>
    </div>
  );
};
